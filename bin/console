#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use Console\HelloCommand;
use Console\TestCommand;

$app = new Application();
$app->add(new HelloCommand());
$app->add(new TestCommand());
$app->run();